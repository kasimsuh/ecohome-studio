export function SiteFooter() {
  return (
    <footer className="border-t border-[color:var(--border)] py-8">
      <div className="shell flex flex-col gap-3 text-sm text-[color:var(--muted)] md:flex-row md:items-center md:justify-between">
        <p>EcoHome Studio for climate-conscious home concept exploration.</p>
        <p>Mocked AI flows stay in place so you can iterate on the experience first.</p>
      </div>
    </footer>
  );
}
